# Codex Client Interactions

Records of attempts to install the Supermemory MCP server using the `codex` client parameter.

- `install_oauth_fail.md` – installation aborted because the `codex` client is not supported by the installer.
