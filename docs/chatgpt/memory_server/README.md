# MCP Server Attempts

Documentation of efforts to configure and authenticate various Model Context Protocol (MCP) servers, such as Supermemory and the local filesystem server, with different clients.

- `claude/` – interactions using the `claude` client.
- `codex/` – interactions using the `codex` client.
- `chatgpt/` – failed attempts with an unsupported `chatgpt` client.
