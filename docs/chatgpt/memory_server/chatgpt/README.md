# ChatGPT Client Attempts

Documentation of efforts to install the Supermemory MCP server using a `chatgpt` client. These attempts fail because `chatgpt` is not among the supported clients for the installer.

- `install_oauth_fail.md` – command issued with OAuth enabled that reports the client as invalid.
