# Filesystem MCP Installation

- **Command**
  - `npx -y install-mcp@latest filesystem --client claude --oauth=no --yes`
- **Result**
  - Installation completed successfully.
  - Filesystem MCP server registered in the Claude configuration.
- **Purpose**
  - Enables organizing and analyzing local files via the Model Context Protocol.
